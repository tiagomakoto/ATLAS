# Implementação do Módulo de Schema

Vou implementar o módulo schema.py conforme especificado na v1. Este módulo será responsável por:

1. Criar todas as tabelas necessárias nos bancos de dados
2. Usar CREATE TABLE IF NOT EXISTS para garantir idempotência
3. Seguir exatamente as especificações de tabelas da seção 4 do documento v1

A implementação seguirá exatamente o que foi especificado no documento v1, na seção 6 (MÓDULO db/schema.py).